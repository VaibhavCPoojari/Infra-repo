apiVersion: v1
kind: Service
metadata:
  name: {{ include "image-annotation-tool.fullname" . }}
  labels:
    {{- include "image-annotation-tool.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "image-annotation-tool.selectorLabels" . | nindent 4 }}
