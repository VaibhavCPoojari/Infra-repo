apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "image-annotation-tool.fullname" . }}-test-connection"
  labels:
    {{- include "image-annotation-tool.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "image-annotation-tool.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
